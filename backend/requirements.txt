# requirements.txt
# Версии зафиксированы и реально протестированы вместе (FastAPI + SQLAlchemy 2.0 async
# + Alembic async env.py + Celery 5.6 + Redis) — см. историю разработки Этапов 1-2.
# Обновлять мажорные версии только с повторной проверкой миграций/async-контекста.

# --- Web framework ---
fastapi==0.139.0
uvicorn[standard]==0.49.0
slowapi==0.1.9

# --- База данных / ORM / миграции ---
sqlalchemy==2.0.51
alembic==1.18.5
asyncpg==0.31.0            # async-драйвер для FastAPI (app/db/session.py: async_engine)
psycopg[binary]==3.3.4     # sync-драйвер для Celery-тасок (app/db/session.py: sync_engine)

# --- Конфигурация ---
pydantic==2.13.4
pydantic-settings==2.14.2

# --- Очереди / фоновые задачи ---
celery[redis]==5.6.3
redis==6.4.0

# --- Внешние интеграции ---
openai==2.44.0              # генерация текста отзыва (gpt-4o-mini), Этап 3
httpx==0.28.1                # Green API, Telegram Bot API, Kaspi Pay — единый HTTP-клиент
supabase==2.31.0             # Supabase Auth + API clients (Python equivalent of @supabase/server)